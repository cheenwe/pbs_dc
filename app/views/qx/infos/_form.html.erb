<%= form_with(model: qx_info, local: true) do |form| %>
  <% if qx_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qx_info.errors.count, "error") %> prohibited this qx_info from being saved:</h2>

      <ul>
      <% qx_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :up_at %>
    <%= form.datetime_select :up_at %>
  </div>

  <div class="field">
    <%= form.label :down_at %>
    <%= form.datetime_select :down_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

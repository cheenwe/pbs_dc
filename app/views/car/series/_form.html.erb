<%= form_with(model: car_series, local: true) do |form| %>
  <% if car_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_series.errors.count, "error") %> prohibited this car_series from being saved:</h2>

      <ul>
      <% car_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand_id %>
    <%= form.number_field :brand_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

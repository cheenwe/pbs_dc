<%= form_with(model: car_config, local: true) do |form| %>
  <% if car_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_config.errors.count, "error") %> prohibited this car_config from being saved:</h2>

      <ul>
      <% car_config.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: tyc_cid, local: true) do |form| %>
  <% if tyc_cid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tyc_cid.errors.count, "error") %> prohibited this tyc_cid from being saved:</h2>

      <ul>
      <% tyc_cid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city_id %>
    <%= form.number_field :city_id %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.number_field :state %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: tyc_investe_abroad, local: true) do |form| %>
  <% if tyc_investe_abroad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tyc_investe_abroad.errors.count, "error") %> prohibited this tyc_investe_abroad from being saved:</h2>

      <ul>
      <% tyc_investe_abroad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.number_field :company_id %>
  </div>

  <div class="field">
    <%= form.label :abroadd_company_id %>
    <%= form.number_field :abroadd_company_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

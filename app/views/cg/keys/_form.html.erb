<%= form_with(model: cg_key, local: true) do |form| %>
  <% if cg_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cg_key.errors.count, "error") %> prohibited this cg_key from being saved:</h2>

      <ul>
      <% cg_key.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :remark %>
    <%= form.text_field :remark %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

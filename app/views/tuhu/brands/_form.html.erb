<%= form_with(model: tuhu_brand, local: true) do |form| %>
  <% if tuhu_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tuhu_brand.errors.count, "error") %> prohibited this tuhu_brand from being saved:</h2>

      <ul>
      <% tuhu_brand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :logo %>
    <%= form.text_field :logo %>
  </div>

  <div class="field">
    <%= form.label :abbr %>
    <%= form.text_field :abbr %>
  </div>

  <div class="field">
    <%= form.label :reamrk %>
    <%= form.text_field :reamrk %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

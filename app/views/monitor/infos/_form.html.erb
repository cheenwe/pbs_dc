<%= form_with(model: monitor_info, local: true) do |form| %>
  <% if monitor_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(monitor_info.errors.count, "error") %> prohibited this monitor_info from being saved:</h2>

      <ul>
      <% monitor_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :server_id %>
    <%= form.number_field :server_id %>
  </div>

  <div class="field">
    <%= form.label :info %>
    <%= form.text_area :info %>
  </div>

  <div class="field">
    <%= form.label :remark %>
    <%= form.text_field :remark %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
